apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "megarepo.fullname" . }}
  labels:
    {{- include "megarepo.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "megarepo.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "megarepo.labels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}
              protocol: TCP
          env:
            - name: SPRING_DATASOURCE_URL
              value: {{ include "megarepo.datasourceUrl" . | quote }}
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ include "megarepo.databaseUser" . | quote }}
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  {{- if .Values.postgresql.enabled }}
                  name: {{ include "megarepo.fullname" . }}-postgresql
                  key: password
                  {{- else }}
                  name: {{ include "megarepo.fullname" . }}-db-secret
                  key: password
                  {{- end }}
            {{- if .Values.springConfig }}
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            {{- end }}
            {{- if .Values.outboundProxy.enabled }}
            - name: MEGAREPO_OUTBOUNDPROXY_ENABLED
              value: "true"
            - name: MEGAREPO_OUTBOUNDPROXY_HOST
              value: {{ required "outboundProxy.host is required when outboundProxy.enabled" .Values.outboundProxy.host | quote }}
            - name: MEGAREPO_OUTBOUNDPROXY_PORT
              value: {{ .Values.outboundProxy.port | quote }}
            {{- if .Values.outboundProxy.username }}
            - name: MEGAREPO_OUTBOUNDPROXY_USERNAME
              value: {{ .Values.outboundProxy.username | quote }}
            {{- end }}
            {{- if .Values.outboundProxy.existingSecret }}
            - name: MEGAREPO_OUTBOUNDPROXY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.outboundProxy.existingSecret }}
                  key: {{ .Values.outboundProxy.passwordKey | default "proxy-password" }}
            {{- else if .Values.outboundProxy.password }}
            - name: MEGAREPO_OUTBOUNDPROXY_PASSWORD
              value: {{ .Values.outboundProxy.password | quote }}
            {{- end }}
            {{- if .Values.outboundProxy.nonProxyHosts }}
            - name: MEGAREPO_OUTBOUNDPROXY_NONPROXYHOSTS
              value: {{ join "," .Values.outboundProxy.nonProxyHosts | quote }}
            {{- end }}
            {{- end }}
            {{- if (.Values.auth).accessTokenExpiry }}
            - name: MEGAREPO_SECURITY_JWT_ACCESS_TOKEN_EXPIRY
              value: {{ .Values.auth.accessTokenExpiry | quote }}
            {{- end }}
            {{- if (.Values.auth).refreshTokenExpiry }}
            - name: MEGAREPO_SECURITY_JWT_REFRESH_TOKEN_EXPIRY
              value: {{ .Values.auth.refreshTokenExpiry | quote }}
            {{- end }}
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
            {{- if .Values.springConfig }}
            - name: spring-config
              mountPath: /app/config
              readOnly: true
            {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim | default (printf "%s-data" (include "megarepo.fullname" .)) }}
        {{- end }}
        {{- if .Values.springConfig }}
        - name: spring-config
          configMap:
            name: {{ include "megarepo.fullname" . }}-config
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
