apiVersion: v1
kind: Service
metadata:
  name: {{ include "megarepo.fullname" . }}
  labels:
    {{- include "megarepo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "megarepo.selectorLabels" . | nindent 4 }}
